@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockListItem>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;

@{
    var content = (ContentModels.TrustedSection)Model.Content;
}

<section class="trusted-section bg-white-100 py-24">
    <div class="container mx-auto max-w-6xl flex flex-row md:flex-row items-center">
        <div>
            <span>@content.TrustedSectionLabel</span>
            <h2>@content.TrustedSectionTitle</h2>
            <p>@content.TrustedSectionDescription</p>
            <div class="star-icon flex">
                @foreach (var _ in Enumerable.Range(0, 5))
                {
                    <i class="fa-solid fa-star text-white-100 text-lg mt-1 mr-2"></i>
                }
            </div>
            <span>@content.TrustedSectionName</span>
            <span>@content.TrustedSectionCompany</span>
        </div>
        <div>
            @{
                System.Diagnostics.Debug.Assert(content.TrustedSectionImage != null, "content.TrustedSectionImage != null");
            }
            <img src="@content.TrustedSectionImage.Url()" alt="A trusted person">
        </div>
    </div>

</section>